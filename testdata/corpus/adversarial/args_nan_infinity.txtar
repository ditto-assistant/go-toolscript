Tool arguments containing NaN.
-- script.js --
const page = parseInt("next");
try { return mcp.search.run({page}); } catch (e) { return [e.name, e.message]; }
-- tools.json --
{
  "search.run": null
}
